# Valores locales de desarrollo. Cambia la contraseña antes de exponer el servicio.
POSTGRES_USER=reservations
POSTGRES_PASSWORD=reservations_local_only
POSTGRES_DB=reservations
DATABASE_URL=postgres://reservations:reservations_local_only@localhost:5432/reservations
PORT=3000
